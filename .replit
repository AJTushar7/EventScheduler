modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Angular Calendar App"

[[workflows.workflow]]
name = "Angular Calendar App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm install -g @angular/cli && npm init -y && npm install @angular/core @angular/platform-browser @angular/platform-browser-dynamic @angular/common @angular/forms rxjs zone.js typescript && ng serve --host 0.0.0.0 --port 5000 --disable-host-check"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "npm install -g @angular/cli && npm init -y && npm install @angular/core @angular/platform-browser @angular/platform-browser-dynamic @angular/common @angular/forms rxjs zone.js typescript && ng serve --host 0.0.0.0 --port 5000 --disable-host-check"]
